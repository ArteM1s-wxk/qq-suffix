# QQ 频道自动后缀工具 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个 Windows 桌面小工具，在 QQ 客户端窗口按回车发送消息前自动追加可配置后缀，并提供 tkinter 窗口修改后缀、启停功能。

**Architecture:** tkinter 主线程跑 GUI，`keyboard` 库在后台线程跑全局键盘钩子拦截回车；用 `win32gui`+`psutil` 判断前台窗口是否为 QQ 进程；后缀持久化到 `config.json`。纯逻辑用 pytest 覆盖，GUI 与真实键盘钩子靠手工验证。

**Tech Stack:** Python 3.11、`keyboard`（全局键盘钩子）、`pywin32`（窗口句柄/进程）、`psutil`（进程名）、`tkinter`（GUI）、`pytest`（测试）、`uv`（环境与依赖）、`git`/`gh`（版本与托管）。

**Spec:** `docs/superpowers/specs/2026-08-14-qq-suffix-design.md`

## Global Constraints

- Python 版本：`>=3.11`，用 uv 锁定到 `3.11`（对 `keyboard` 这类老库兼容性最好）。
- 项目根目录：`D:\Python-Working\qq-suffix`；源码在 `src/qq_suffix/`，测试在 `tests/`。
- 默认后缀：`音音`。
- QQ 进程名：`QQ.exe`（大小写不敏感匹配）。
- `config.json` 为运行时生成，加入 `.gitignore`，不提交。
- 提交信息用中文，前缀用 conventional commit（`feat:`/`chore:`/`test:`）。

---

## File Structure

| 文件 | 职责 |
|------|------|
| `pyproject.toml` | 项目元数据、依赖、pytest 配置 |
| `.python-version` | uv 锁定的 Python 版本（`3.11`） |
| `.gitignore` | 忽略 `.venv/`、`__pycache__/`、`config.json` 等 |
| `src/qq_suffix/__init__.py` | 包标记（空） |
| `src/qq_suffix/config.py` | 后缀的读写与默认值 |
| `src/qq_suffix/window.py` | 判断前台窗口是否为 QQ 进程 |
| `src/qq_suffix/listener.py` | 回车拦截、后缀追加的决策与钩子逻辑 |
| `src/qq_suffix/app.py` | tkinter GUI 与程序入口 |
| `src/qq_suffix/__main__.py` | `python -m qq_suffix` 入口 |
| `tests/test_config.py` | config 单元测试 |
| `tests/test_window.py` | window 纯函数测试 |
| `tests/test_listener.py` | listener 决策函数测试 |

---

### Task 1: 初始化项目脚手架（uv + git）

**Files:**
- Create: `pyproject.toml`
- Create: `.python-version`
- Create: `.gitignore`
- Create: `src/qq_suffix/__init__.py`

**Interfaces:**
- Produces: 可被 `uv sync` 安装、`uv run pytest` 可发现 `src/qq_suffix` 包的项目骨架。

- [ ] **Step 1: 写 pyproject.toml**

```toml
[project]
name = "qq-suffix"
version = "0.1.0"
description = "QQ 频道自动追加后缀工具"
requires-python = ">=3.11"
dependencies = [
    "keyboard>=0.13.5",
    "pywin32>=306",
    "psutil>=5.9.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qq_suffix"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
```

- [ ] **Step 2: 写 .python-version**

内容一行：

```
3.11
```

- [ ] **Step 3: 写 .gitignore**

```
.venv/
__pycache__/
*.pyc
.pytest_cache/
config.json
```

- [ ] **Step 4: 创建包标记文件**

创建 `src/qq_suffix/__init__.py`，内容为空。

- [ ] **Step 5: 同步依赖**

```bash
cd /d/Python-Working/qq-suffix && uv sync
```

Expected: 创建 `.venv/`，安装 `keyboard`、`pywin32`、`psutil`、`pytest`，无报错。

- [ ] **Step 6: 初始化 git 并首次提交**

```bash
cd /d/Python-Working/qq-suffix && git init && git add pyproject.toml .python-version .gitignore src/qq_suffix/__init__.py && git commit -m "chore: 初始化项目脚手架（uv + git）"
```

---

### Task 2: config 配置模块

**Files:**
- Create: `src/qq_suffix/config.py`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces:
  - `DEFAULT_SUFFIX: str = "音音"`
  - `load_suffix(path: Path) -> str` — 文件不存在或损坏时返回 `DEFAULT_SUFFIX`。
  - `save_suffix(path: Path, suffix: str) -> None` — 写入 JSON `{"suffix": ...}`。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_config.py`：

```python
from qq_suffix.config import DEFAULT_SUFFIX, load_suffix, save_suffix


def test_load_suffix_returns_default_when_missing(tmp_path):
    assert load_suffix(tmp_path / "config.json") == DEFAULT_SUFFIX


def test_save_and_load_roundtrip(tmp_path):
    path = tmp_path / "config.json"
    save_suffix(path, "你好")
    assert load_suffix(path) == "你好"


def test_load_suffix_returns_default_on_invalid_json(tmp_path):
    path = tmp_path / "config.json"
    path.write_text("{not json", encoding="utf-8")
    assert load_suffix(path) == DEFAULT_SUFFIX


def test_load_suffix_returns_default_on_empty_string(tmp_path):
    path = tmp_path / "config.json"
    save_suffix(path, "")
    assert load_suffix(path) == DEFAULT_SUFFIX
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /d/Python-Working/qq-suffix && uv run pytest tests/test_config.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'qq_suffix.config'`。

- [ ] **Step 3: 写最小实现**

创建 `src/qq_suffix/config.py`：

```python
import json
from pathlib import Path

DEFAULT_SUFFIX = "音音"


def load_suffix(path: Path) -> str:
    if not path.exists():
        return DEFAULT_SUFFIX
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        return DEFAULT_SUFFIX
    suffix = data.get("suffix")
    if isinstance(suffix, str) and suffix:
        return suffix
    return DEFAULT_SUFFIX


def save_suffix(path: Path, suffix: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps({"suffix": suffix}, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /d/Python-Working/qq-suffix && uv run pytest tests/test_config.py -v
```

Expected: 4 passed。

- [ ] **Step 5: 提交**

```bash
cd /d/Python-Working/qq-suffix && git add src/qq_suffix/config.py tests/test_config.py && git commit -m "feat: 新增配置读写模块"
```

---

### Task 3: window 窗口识别模块

**Files:**
- Create: `src/qq_suffix/window.py`
- Test: `tests/test_window.py`

**Interfaces:**
- Consumes: 无（依赖已装的 `pywin32`、`psutil`）。
- Produces:
  - `QQ_PROCESS_NAME: str = "QQ.exe"`
  - `is_qq_process(process_name: str) -> bool` — 纯函数，大小写不敏感。
  - `current_foreground_process_name() -> str | None` — 取前台进程名，失败返回 `None`。
  - `is_qq_foreground() -> bool` — 前台是否 QQ。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_window.py`：

```python
from qq_suffix.window import is_qq_process


def test_is_qq_process_matches_case_insensitively():
    assert is_qq_process("QQ.exe") is True
    assert is_qq_process("qq.exe") is True
    assert is_qq_process("Qq.Exe") is True


def test_is_qq_process_returns_false_for_others():
    assert is_qq_process("chrome.exe") is False
    assert is_qq_process("") is False
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /d/Python-Working/qq-suffix && uv run pytest tests/test_window.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'qq_suffix.window'`。

- [ ] **Step 3: 写最小实现**

创建 `src/qq_suffix/window.py`：

```python
from __future__ import annotations

import psutil
import win32gui
import win32process

QQ_PROCESS_NAME = "QQ.exe"


def is_qq_process(process_name: str) -> bool:
    return process_name.lower() == QQ_PROCESS_NAME.lower()


def current_foreground_process_name() -> str | None:
    try:
        hwnd = win32gui.GetForegroundWindow()
        _, pid = win32process.GetWindowThreadProcessId(hwnd)
        return psutil.Process(pid).name()
    except Exception:
        return None


def is_qq_foreground() -> bool:
    name = current_foreground_process_name()
    return name is not None and is_qq_process(name)
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /d/Python-Working/qq-suffix && uv run pytest tests/test_window.py -v
```

Expected: 2 passed。

- [ ] **Step 5: 提交**

```bash
cd /d/Python-Working/qq-suffix && git add src/qq_suffix/window.py tests/test_window.py && git commit -m "feat: 新增 QQ 窗口识别模块"
```

---

### Task 4: listener 键盘监听模块

**Files:**
- Create: `src/qq_suffix/listener.py`
- Test: `tests/test_listener.py`

**Interfaces:**
- Consumes: `keyboard` 库；`window.is_qq_foreground()`（Task 3）。
- Produces:
  - `should_append(enabled: bool, is_qq: bool) -> bool` — 纯函数，`enabled and is_qq`。
  - `class Listener`：`__init__(get_suffix, is_qq_window)`、`start()`、`stop()`、`set_enabled(bool)`、`enabled`（属性）。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_listener.py`：

```python
from qq_suffix.listener import should_append


def test_should_append_only_when_enabled_and_qq():
    assert should_append(True, True) is True
    assert should_append(False, True) is False
    assert should_append(True, False) is False
    assert should_append(False, False) is False
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /d/Python-Working/qq-suffix && uv run pytest tests/test_listener.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'qq_suffix.listener'`。

- [ ] **Step 3: 写实现**

创建 `src/qq_suffix/listener.py`：

```python
from __future__ import annotations

import threading
from collections.abc import Callable

import keyboard


def should_append(enabled: bool, is_qq: bool) -> bool:
    return enabled and is_qq


class Listener:
    def __init__(
        self,
        get_suffix: Callable[[], str],
        is_qq_window: Callable[[], bool],
    ) -> None:
        self._get_suffix = get_suffix
        self._is_qq_window = is_qq_window
        self._enabled = threading.Event()
        self._hook = None

    def start(self) -> None:
        if self._hook is None:
            self._hook = keyboard.on_press_key("enter", self._on_enter, suppress=True)

    def stop(self) -> None:
        if self._hook is not None:
            keyboard.unhook(self._hook)
            self._hook = None

    def set_enabled(self, value: bool) -> None:
        if value:
            self._enabled.set()
        else:
            self._enabled.clear()

    @property
    def enabled(self) -> bool:
        return self._enabled.is_set()

    def _on_enter(self, _event) -> None:
        # 回车被全局抑制，需临时移除 hook 再补发，否则模拟的 enter 会被再次抑制。
        keyboard.unhook(self._hook)
        try:
            if should_append(self._enabled.is_set(), self._is_qq_window()):
                keyboard.write(self._get_suffix())
            keyboard.send("enter")
        finally:
            self._hook = keyboard.on_press_key("enter", self._on_enter, suppress=True)
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /d/Python-Working/qq-suffix && uv run pytest tests/test_listener.py -v
```

Expected: 1 passed。

- [ ] **Step 5: 提交**

```bash
cd /d/Python-Working/qq-suffix && git add src/qq_suffix/listener.py tests/test_listener.py && git commit -m "feat: 新增键盘监听与后缀追加逻辑"
```

---

### Task 5: GUI 与程序入口

**Files:**
- Create: `src/qq_suffix/app.py`
- Create: `src/qq_suffix/__main__.py`

**Interfaces:**
- Consumes: `config.load_suffix`/`save_suffix`、`listener.Listener`、`window.is_qq_foreground`。
- Produces: `app.main()` — 创建 tkinter 窗口、启动监听、进入主循环、退出时停止监听。

- [ ] **Step 1: 写 app.py**

创建 `src/qq_suffix/app.py`：

```python
from __future__ import annotations

import tkinter as tk
from pathlib import Path

from qq_suffix.config import load_suffix, save_suffix
from qq_suffix.listener import Listener
from qq_suffix.window import is_qq_foreground

PROJECT_ROOT = Path(__file__).resolve().parents[2]
CONFIG_PATH = PROJECT_ROOT / "config.json"


class App:
    def __init__(self, root: tk.Tk, listener: Listener, config_path: Path) -> None:
        self.root = root
        self.listener = listener
        self.config_path = config_path

        root.title("QQ 频道自动后缀")
        root.resizable(False, False)

        self.suffix_var = tk.StringVar(value=load_suffix(config_path))
        self.status_var = tk.StringVar(value="已停止")

        tk.Label(root, text="后缀内容：").grid(row=0, column=0, padx=8, pady=8, sticky="w")
        self.entry = tk.Entry(root, textvariable=self.suffix_var, width=30)
        self.entry.grid(row=0, column=1, padx=8, pady=8)

        self.toggle_btn = tk.Button(root, text="启动", command=self.toggle)
        self.toggle_btn.grid(row=1, column=0, padx=8, pady=8)

        self.status_label = tk.Label(root, textvariable=self.status_var, fg="red")
        self.status_label.grid(row=1, column=1, padx=8, pady=8, sticky="w")

        self.suffix_var.trace_add("write", self._on_suffix_change)

    def toggle(self) -> None:
        if self.listener.enabled:
            self.listener.set_enabled(False)
            self.status_var.set("已停止")
            self.toggle_btn.config(text="启动")
            self.status_label.config(fg="red")
        else:
            self.listener.set_enabled(True)
            self.status_var.set("运行中")
            self.toggle_btn.config(text="停止")
            self.status_label.config(fg="green")

    def _on_suffix_change(self, *_args) -> None:
        save_suffix(self.config_path, self.suffix_var.get())


def main() -> None:
    root = tk.Tk()
    listener = Listener(
        get_suffix=lambda: load_suffix(CONFIG_PATH),
        is_qq_window=is_qq_foreground,
    )
    listener.start()
    App(root, listener, CONFIG_PATH)
    root.mainloop()
    listener.stop()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 写 __main__.py**

创建 `src/qq_suffix/__main__.py`：

```python
from qq_suffix.app import main

if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 确认模块可导入、无语法错误**

```bash
cd /d/Python-Working/qq-suffix && uv run python -c "import qq_suffix.app; import qq_suffix.window; import qq_suffix.listener; print('import ok')"
```

Expected: 输出 `import ok`（若机器无图形会话，tkinter 的 `tk.Tk()` 不会在此步执行，仅 import，因此不会报错）。

- [ ] **Step 4: 跑全量测试确认无回归**

```bash
cd /d/Python-Working/qq-suffix && uv run pytest -v
```

Expected: 7 passed。

- [ ] **Step 5: 提交**

```bash
cd /d/Python-Working/qq-suffix && git add src/qq_suffix/app.py src/qq_suffix/__main__.py && git commit -m "feat: 新增 tkinter GUI 与程序入口"
```

---

### Task 6: 创建 GitHub 私有仓库并推送

**Files:**
- 无代码改动。

- [ ] **Step 1: 创建私有仓库**

```bash
cd /d/Python-Working/qq-suffix && gh repo create qq-suffix --private --source=. --remote=origin --push
```

Expected: 创建 `ArteM1s-wxk/qq-suffix` 私有仓库并推送全部提交。

- [ ] **Step 2: 确认远程与分支**

```bash
cd /d/Python-Working/qq-suffix && git remote -v && git branch --show-current
```

Expected: `origin` 指向 `github.com/ArteM1s-wxk/qq-suffix`，当前分支为默认分支（`main` 或 `master`）。

---

### Task 7: 端到端手工验证

**Files:**
- 无代码改动。

- [ ] **Step 1: 启动程序**

```bash
cd /d/Python-Working/qq-suffix && uv run python -m qq_suffix
```

Expected: 弹出「QQ 频道自动后缀」窗口，后缀框显示「音音」，状态「已停止」。

- [ ] **Step 2: 验证启停与后缀修改**

在 GUI 中把后缀改成测试字符串（如「测试」），点击「启动」，状态变「运行中」；点「停止」变「已停止」。

- [ ] **Step 3: 验证 QQ 内追加**

启动状态下，在 QQ 输入框输入内容后按回车，确认消息末尾自动带「测试」，且回车正常发送。

- [ ] **Step 4: 验证非 QQ 窗口不受影响**

在记事本按回车，确认正常换行、无多余后缀。

- [ ] **Step 5: 验证持久化**

关闭程序后重启，确认后缀框恢复为上次修改的值（「测试」）。

- [ ] **Step 6: 验证停止后不再追加**

停止状态下在 QQ 发消息，确认不再追加后缀。

---

## Self-Review 结论

- **Spec 覆盖**：功能需求（自动追加、GUI、持久化、暂停）→ Task 4/5/2；非功能（仅 QQ 生效、停止无副作用）→ Task 3/4；验证计划 → Task 7。均覆盖。
- **占位符**：无 TBD/TODO/「适当处理」类描述，所有代码步骤含完整代码。
- **类型一致性**：`load_suffix`/`save_suffix`/`should_append`/`Listener`/`is_qq_foreground` 在 Task 2-5 中签名一致；`CONFIG_PATH` 用 `parents[2]` 正确指向项目根。
